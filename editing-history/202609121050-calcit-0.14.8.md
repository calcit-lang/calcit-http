# Calcit 0.14.8 / Calcit 0.14.8

## 中文

- 将项目要求的 Calcit 版本从 0.14.7 升级到 0.14.8，保持 HTTP C-safe FFI 契约与模块版本不变。
- `--strict-types --check-only`、带 baseline 的静态质量门禁与文档检查通过。
- 将 CI 的 `actions/checkout`、`calcit-lang/setup-calcit`、`Swatinem/rust-cache` 固定到不可变 commit。

## English

- Upgrade the required Calcit version from 0.14.7 to 0.14.8 while preserving the HTTP C-safe FFI contract and module version.
- `--strict-types --check-only`, the baseline quality gate, and doc checks pass.
- Pin `actions/checkout`, `calcit-lang/setup-calcit`, and `Swatinem/rust-cache` in CI to immutable commits.

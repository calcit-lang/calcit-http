# Calcit 0.14.7 strict migration

- Raise the project toolchain requirement to Calcit 0.14.7.
- Mark the five heterogeneous HTTP EDN request/response slots as an intentional, narrow FFI boundary, reducing unresolved type debt from five occurrences to zero without widening the public API.
- Replace the native smoke `assert=` call affected by calcit-lang/calcit#951 with a typed `String` equality assertion.
- Regenerate the quality baseline in the v2 per-definition Cirru format, including the `unsafeCoerce` budget.

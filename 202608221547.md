# 2026-08-22 15:47

- 更新开发指南，统一使用 `calcit.cirru` 与当前 `calcit` CLI，移除过时的 `cr`/`compact.cirru` 流程。
- 将文档中的 `cirru_edn` 兼容约束校正为当前实际使用的 0.8.0，并保留 Rust ABI 与动态库回归步骤。
- 为 deps.cirru 补充模块版本，并让 CI 显式安装 `calcit` 与 `caps` 工具。
